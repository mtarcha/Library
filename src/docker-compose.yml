version: '3.4'

services:
  library.api:
    image: ${DOCKER_REGISTRY-}libraryapi
    build:
      context: .
      dockerfile: Library.Api/Dockerfile  

  library.presentation.mvc:
    image: ${DOCKER_REGISTRY-}librarypresentationmvc
    build:
      context: .
      dockerfile: Library.Presentation.MVC/Dockerfile   